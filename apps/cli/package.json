{
  "name": "@sshai/cli",
  "private": true,
  "scripts": {
    "build": "go build ./...",
    "format:check": "test -z \"$(gofmt -l .)\"",
    "generate": "go generate ./...",
    "lint": "go vet ./...",
    "test": "go test ./...",
    "test:race": "go test -race ./..."
  }
}
