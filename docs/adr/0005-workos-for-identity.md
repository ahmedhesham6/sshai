# Use WorkOS AuthKit for web and CLI identity

WorkOS AuthKit is the identity authority because it supports hosted authentication, TanStack Start, Go, JWT validation, and OAuth device authorization for the CLI. The application stores a local User projection keyed by WorkOS user ID and does not implement passwords, social login, refresh-token issuance, or organizations in the MVP.
