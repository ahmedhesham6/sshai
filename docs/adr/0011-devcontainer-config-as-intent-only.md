# Treat devcontainer.json as intent only

devcontainer.json is consumed as detected intent that feeds the project Capsule, never adopted as the Environment model; the product retains durable-VM Environments, a security boundary on lifecycle commands, and the standard SSH client contract.
