# Compile Profiles from explicit allowlists into immutable versions

Local discovery suggests portable configuration, but only user-selected paths and selectors enter a Profile. Published Profile Versions are immutable and Environments pin them until explicit upgrade, preventing ambiguous authority between multiple laptops, the cloud, and running Environments.
