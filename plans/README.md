# Implementation Plans

Generated by the improve skill on 2026-07-17 (audit focus: directory
structure; planned at commit `c2c4e2d`). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

**Repo-wide caution for every executor**: parallel sessions actively develop
in this repo (ReserveInitialRuntime work touching
`apps/workflows/environment_create*`, `libs/domain/environment.go`,
`libs/db` environment-creation files). Three test failures pre-exist from
that WIP — each plan lists them. Never start a plan over uncommitted changes
in its scope.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 005 | Hygiene: `.tanstack/` ignore, capsule-store tftest, root `AGENTS.md` | P1 | S | — | DONE except tftest (reviewed; merged to main @ `5382234`, pushed. Tftest sub-item BLOCKED: `terraform` not installed in this environment; executor verified all 7 resources support the planned assertions — re-run step 2 of the plan once terraform is available) |
| 001 | Move materialization/drift vocabulary to `libs/profile` (dissolves `workflows → guest`) | P1 | M | — | DONE (reviewed; merged to main @ `d7db9a4`, pushed) |
| 003 | CLI entrypoint under `apps/cli/cmd/devm/` | P2 | S | — | DONE (reviewed; merged to main @ `4163c3c`, pushed) |
| 002 | Extract adapters to `libs/adapters` | P2 | L | 001 | DONE (reviewed; gate-tripped once, re-scoped +8 shared helpers to libs/profile; merged to main @ `953c73c`, pushed) |
| 004 | Reconcile layout docs (spec 16, spec 13) with the tree | P2 | S | 001, 003; 002 resolved (DONE or REJECTED) | DONE (reviewed; merged to main @ `248f362`, pushed) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned)

## Dependency notes

- **002 requires 001**: the adapter `Translate` signature uses
  `InstalledMaterialization` and friends; 001 moves that vocabulary to
  `libs/profile` first so 002's seam can be expressed in `profile.*` types.
- **004 runs last**: it documents the tree as it stands, so the code moves
  (001, 003, and 002's outcome either way) must be settled first. If 002 is
  REJECTED, 004 documents adapters as "planned — currently in apps/guest".
- **005, 001, 003 are mutually independent** and safe to run in parallel
  *provided* each runs on its own branch; 003 and 005 both touch `.gitignore`
  (different lines — trivial merge).

---

# Spec-completion slices (goal 2026-07-17)

Derived from `spec-gap-matrix.md`. Foundation-first; each slice runs the full
protocol: worktree implementation → verification → quality-gate loop (Codex
adversarial + /code-review high + /simplify) until zero verified findings →
fetch-sync-merge-push.

## Ratified rulings (2026-07-18 grilling session)

All previously-parked product decisions on the E2E critical path are now
ruled — see `docs/spec/18-decision-register.md` ("Amendments"), the updated
`docs/spec/17-open-decisions.md`, and ADRs 0012/0013. Headlines binding the
slices below:

- **Order**: E2E first (S2 → S4 → S5 → S6), then breadth (S7–S13).
- **Verification mode (amended 2026-07-18)**: fakes-first — no credentials
  exist yet, so every slice verifies with the established test-fake/local
  pattern (fakes in-package, local Restate in Docker where useful). Target
  region for all provider config stays `eu-central-1`/`eu-central-1a`. When
  the maintainer provisions `~/.sshai-deploy.env` (AWS, Restate Cloud,
  WorkOS, Polar; 0600; smoke-verify, never echo), a dedicated deployment
  phase runs terraform/packer and a live create→ssh→stop smoke E2E.
- **Credits**: starts refuse at balance ≤ 0 (`CREDITS_POLICY_BLOCKED`);
  never force-stop running compute in alpha.
- **Auto-stop defaults**: `when_fully_idle`, 300s grace, 60s cadence, 300s
  stale threshold. Guest unknown-process + cgroup signals pulled forward
  into the S2 arc (IDE detection stays in S8).
- **Sizing**: preset ladder `cpu2-mem8`…`cpu16-mem64` → m7i-flex;
  user-configurable data volume (default 100 GiB, 20–500); system volume
  30 GiB platform-owned.
- **Images/agents**: weekly pinned AMI; upgrade-on-start (start→replace on
  outdated AMI); home-first user tooling; system-path installs ephemeral
  (ADR 0013).
- **Tags**: Postgres tag index written by `capsule publish` (ADR 0012).
- **Contract**: `capsuleLockId` nullable (regenerate Go + TS clients when
  touching it); `@sshai/contracts` kept, wired in S11.

| Slice | Title | Depends on | Status |
|-------|-------|------------|--------|
| S0 | Quick wins: DX docs (CLI env vars, dev loop, README quickstart); go.mod toolchain task dropped (redundant per Go tooling — pin lives in CI gate) | — | DONE (merged @ `f3ad56f`) |
| S1 | Workflow-serving binary (`apps/workflows/cmd/workflows`) — re-scoped to BillingDelivery-only after discovering 3 of 4 services have test-fake-only dependencies (see matrix item 1) | — | DONE (merged @ `c26a20b`; recovered from executor session-limit death mid-run — work verified, committed, and landed by advisor) |
| S1b | Production workflow dependencies — `CapsuleResolver` DONE (`b27e026`, digest-only; tag resolution = named product decision), `db.Store.LoadProfileResolveState` DONE (`70c7087`, merged @ `0eee07d`) | S1 | DONE (partial scope; remainder → S1c) |
| S1c | Composition + prerequisites: production S3 GrantProvider (`libs/capsule/oci/s3grants.go`), read-only pin query (`libs/db/environment_create_pin.go`), `PinnedProfileVersionResolver` with full `ResolveCapsuleComposition` pipeline, EnvironmentCreate + ProfileResolve bound in cmd/workflows | S1b | DONE (merged @ `fc5bbb1`; full quality loop: review→REVISE (composition rules)→adversarial gate (0 live vulns, 2 MED hardening fixed, 2 LOW accepted with refutation)→12/12 tests) |
| S2 | Runtime start/stop workflows + SendRuntimeOperation + control-plane wiring + API endpoints. Amended scope (2026-07-18): + AutoStop production binding with ratified defaults, + guest unknown-process/cgroup signals, + compute-usage-interval open/close wiring, + credit-policy start check, + upgrade-on-start hook point (start→replace decision), toolchain reinstall first (machine currently has no Go) | S1, S1b; ReserveInitialRuntime landed — UNBLOCKED | DONE (2026-07-19; landed as S2a runtime workflows + S2b control-plane endpoints + S2c guest signals + S2d production wiring on `advisor/s2-integration`. Full quality loop: per-branch adversarial gates to ZERO (S2a 5 rounds, S2b 4, S2c 2), integration gate to zero, /code-review high 8-angle pass (10 verified findings, all fixed), final delta gates to ZERO. Carried notes: upgrade-on-start terminally fails with `REPLACE_REQUIRED` until S4 lands the replace workflow (S4 definition-of-done: convert to silent replace per ADR 0013); guest readiness/SSH-reconcile/shutdown transports bound as explicit permanent errors until S5 provides the guest control path; durable `environment.update_auto_stop` workflow remains a recorded requirement (synchronous persist + generation-tracked refresh with recovery delivery is the interim); `capsuleLockId` empty-string→nullable still pending contract regen; activity snapshots pruned at 24h/hourly) |
| S3 | Read models: 9 GET endpoints (environments, profiles, operations, events, /me, /billing) | — | DONE (merged @ `3c713ab`; recovered from second executor session-limit death; gate: 0 authz findings, 3 LOW) |
| S3b | Cursor pagination on the 3 collection endpoints (spec 09) + owner-assertion hardening in handler fakes. Pending maintainer decision: `capsuleLockId` empty-string sentinel vs nullable contract field | S3 | DONE (merged @ `c323143`, pushed; keyset pagination, contract-first, boundary-tested) |
| S4 | environment.create steps 4–11 (EC2 provision/attach/network, guest boot, seed, materialize-from-workflow). Amended scope (2026-07-18): + image-pipeline terraform module + eu-central-1 Packer template with pinned agents + ECS cluster/services terraform (authored + validated offline; actual apply/build waits for the credentialed deployment phase) + user-space package managers preconfigured home-first in the AMI | S1, S2 | DONE (2026-07-19; landed as S4a environment.create + S4b runtime.replace + S4c guest supervisor/control transport + S4d image pipeline/Packer/ECS, then integration on `advisor/s4-integration`. Full quality loop: S4a–S4d each reached zero after one gate-fix round, integration reached zero after one gate-fix round, and the post-landing verified review's 14 findings were fixed. Carried notes: S2's `REPLACE_REQUIRED` path is converted to silent replace DONE per ADR 0013; guest readiness/SSH-reconcile/shutdown transports are real behind guest-control TLS configuration; durable `environment.update_auto_stop` workflow remains owed; `capsuleLockId` nullable contract regeneration remains owed; project-toolchain validation depends on S9 Project Spec work; decision-11/13 remain open inputs in the ECS/RDS modules) |
| S5 | Connection path: POST /connection-intents server side, proxy start-on-connect, CLI ssh config/include wiring | S2, S3 | TODO |
| S6 | CLI lifecycle: bare `devm`, status, stop, delete, doctor, logout, profile noun, capsule publish/inspect/diff | S2, S3 | TODO |
| S7 | Deletion safety flow (specs 05+11): disclosures, exact-name confirm, recent-auth, delete workflow | S1 | TODO |
| S8 | Guest signal completeness: IDE/cgroup/unknown-process sources, stop-op record fidelity, audit telemetry | — | TODO |
| S9 | Project domain: ProjectBinding/ProjectSpec/CredentialRequirement+Binding aggregates, project discovery | — | TODO |
| S10 | Billing completion: storage metering, meter reconciliation, portal/checkout, then web billing surface | S3 | TODO |
| S11 | Web surfaces: wire @sshai/contracts client, environment list/detail, runtime controls, operations | S3 | TODO |
| S12 | Hardening: sshd directives + image build gates + budget controls | — | TODO |
| S13 | Observability: OTel wiring, spec-14 metrics, reconciliation drivers | S1 | TODO |

The ReserveInitialRuntime WIP landed before 2026-07-18 (tree verified clean;
maintainer confirmed the whole critical-path surface is claimable) — the old
S4-overlap caution is retired. Still never start a slice over uncommitted
changes in its scope.

## Findings considered and rejected

So nobody re-audits these:

- **Move `libs/provideraws` → `libs/provider/aws`** (auditor suggestion):
  rejected — `docs/spec/16-implementation-plan.md` explicitly ratifies
  `provider/` and `provideraws/` as sibling packages; the placement is
  documented intent, not drift. The `providertest` nesting asymmetry is
  cosmetic.
- **Enforce layering with `internal/` across libs** : deferred, not planned —
  the import graph is currently clean (only `libs/db/internal/dbsql` exists,
  used correctly), the repo is mid-migration, and boundary enforcement now
  would fight active work. Revisit after plans/002 settles the adapter seam.
- **Split `libs/db` / `libs/domain` flat packages**: deferred — real (42
  files / 35 files), but the parallel ReserveInitialRuntime WIP lives exactly
  there; a split now guarantees conflicts. Re-raise when that work lands.
- **Orphaned `@sshai/contracts` TS client** (generated hey-api SDK with zero
  consumers; `apps/web` neither depends on nor imports it — verified): not
  selected for planning this round. Decision needed from the maintainer:
  wire `apps/web` to it or stop generating the TS half. Recorded here so the
  finding isn't lost.
